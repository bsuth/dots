# ------------------------------------------------------------------------------
# PATH
# ------------------------------------------------------------------------------

MYPATHS=(
	"$HOME/.local/bin"
	"$DOTS/scripts"
	"$HOME/.cargo/bin"
)

for p in "${MYPATHS[@]}"; do
	[[ -d $p ]] && PATH="$PATH:$p"
done

# vim: syntax=zsh
