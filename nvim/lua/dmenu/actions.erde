local { DOTS, PLUGINS_DIR, SWAP_DIR } = require('constants')
local { edit } = require('utils.edit')
local { sudo_write } = require('utils.sudo')

return {
  -- ---------------------------------------------------------------------------
  -- General
  -- ---------------------------------------------------------------------------

  { 'source', () -> vim.cmd('source $MYVIMRC') },
  { 'sudo write', sudo_write },
  { 'syntax refresh', () -> vim.cmd('syntax clear | syntax reset | syntax enable') },
  { 'toggle number', () -> { vim.wo.number = !vim.wo.number } },

  -- ---------------------------------------------------------------------------
  -- Help, Man
  -- ---------------------------------------------------------------------------

  { 'help', () -> print('todo') },
  { 'man', () -> print('todo') },

  -- ---------------------------------------------------------------------------
  -- Quicklinks
  -- ---------------------------------------------------------------------------

  { 'goto root', () -> edit('/') },
  { 'goto dots', () -> edit(DOTS) },
  { 'goto plugins', () -> edit(PLUGINS_DIR) },
  { 'goto swap', () -> edit(SWAP_DIR) },

  -- ---------------------------------------------------------------------------
  -- Plugins
  --
  -- NOTE: Neovim requires commands for multiline printing...
  -- https://github.com/neovim/neovim/issues/5067
  -- ---------------------------------------------------------------------------

  { 'plugins update', () -> vim.cmd('lua require("utils.plugins").update()') },
  { 'plugins clean ', () -> vim.cmd('lua require("utils.plugins").clean()') },
}
